installed 
tailwind vite engine 
axios 
react router dom 
npm install @reduxjs/toolkit 
npm install react-redux(overall state management of a website (if the user is logged in and we can manipulate the components with that same as context api in some concepts))
npm i react-icons


